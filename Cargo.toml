[package]
name = "skip-list-rs"
version = "0.1.0"
edition = "2021"
description = "Skip list with probabilistic balancing, range queries, and iterator support — pure Rust, no dependencies"
license = "MIT OR Apache-2.0"
repository = "https://github.com/SuperInstance/skip-list-rs"
keywords = ["skip-list", "data-structure", "sorted", "probabilistic"]
categories = ["data-structures", "algorithms"]
authors = ["SuperInstance"]
